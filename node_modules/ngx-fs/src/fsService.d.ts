import { Fs } from './fs.interface';
export declare class FsService {
    private _fs;
    private readonly f_s;
    /**
     * determines if SPA is running in Electron
     * @returns {boolean}
     */
    readonly isElectronApp: boolean;
    readonly fs: Fs;
}
